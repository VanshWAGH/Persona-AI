import ChatWindow from "@/components/ChatWindow";

/**
 * Home Page — Renders the full-screen chat interface.
 * This is the single-page application entry point.
 */
export default function Home() {
  return <ChatWindow />;
}
